#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;
using P = pair<int, int>;

int main() {
  cout << fixed << setprecision(10);
  double L;
  cin >> L;
  double l = L / 3;
  double ans = l * l * l;
  cout << ans << endl;
}
