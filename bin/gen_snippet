#!/usr/bin/env node

const raw = `
void print_vec(vector<T> vec) {
  rep(i, vec.size()) {
    cout << vec.at(i);
    if (i == vec.size() - 1) {
      cout << endl;
    } else {
      cout << ' ';
    }
  }
}
`;

const lines = raw.split("\n");
console.log(JSON.stringify(lines, undefined, 2));
