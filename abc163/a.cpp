#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;
using P = pair<int, int>;

int main() {
  cout << fixed << setprecision(10);
  float r;
  cin >> r;
  float ans = 2 * M_PI * r;
  cout << ans << endl;
}
